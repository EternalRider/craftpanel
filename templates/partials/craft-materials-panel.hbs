<div class="craft-panel">
    {{> craft-panel-tittle type="materials" panelSizes=panelSizes isEdit=isEdit}}
    {{#if categories}}{{> craft-category-panel type="materials" categories=categories panelSizes=panelSizes width=width}}{{/if}}
    <div class="craft-materials-panel scroll-log-panel" data-panel="materials" style="width:{{craftpanel-exist panelSizes.materials.width width}}px;height:{{craftpanel-exist panelSizes.materials.height height}}px;">
        {{#each materials as |slot|}}
        <div draggable="true" data-tooltip-class="craft-tooltip" data-tooltip="{{slot.tooltip}}"
            class="element-slot materials" data-uuid="{{slot.uuid}}"
            style="{{#if slot.itemColor}}--item-color: {{slot.itemColor}};{{/if}} {{#if slot.item.img}}background-image: url('{{slot.item.img}}');{{/if}} ">
            <div class="craft-slot-elements" {{#unless slot.showElements}}hidden="true" {{/unless}}{{#if slot.empty}}hidden="true" {{/if}} draggable="false">
                {{#each slot.elements as |element|}}
                <div class="slot-element-circle-container">
                    {{#each (craftpanel-range 0 element.num)}}
                    <i class="slot-element-circle fa-solid fa-{{element.shape}}" draggable="false" style="color: {{element.color}};"></i>
                    {{/each}}
                </div>
                {{/each}}
            </div>
            <div class="craft-slot-quantity" {{#unless slot.showQuantity}}hidden="true"{{/unless}} draggable="false">
                {{slot.quantity}}
            </div>
        </div>
        {{/each}}
    </div>
</div>